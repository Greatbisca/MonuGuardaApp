@model IEnumerable<MonuGuardaApp.Models.ReservaVisita>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Turista)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VisitasGuiadas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataReserva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NPessoas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Turista.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VisitasGuiadas.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataReserva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NPessoas)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReservaVisitaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReservaVisitaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReservaVisitaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
